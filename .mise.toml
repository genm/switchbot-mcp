[tools]
node = "24"

[tasks.setup]
description = "Install dependencies and Git hooks"
run = "npm install && npm run hooks:install"

[tasks.ci]
description = "Run all CI checks"
run = "npm ci --ignore-scripts && npm run check && npm run test:coverage"
